{
  "name": "ollama-tester-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors yellow,cyan \"cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8002\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
